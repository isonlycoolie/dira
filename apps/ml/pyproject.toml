[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "dira-ml"
version = "0.1.0"
description = "DIRA machine learning service."
requires-python = ">=3.12"

[tool.hatch.envs.default]
dependencies = [
  "-e ../../libs/schemas",
  "-e ../../libs/common",
]
